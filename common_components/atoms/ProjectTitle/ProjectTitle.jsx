import { Title } from "./ProjectTitle.styles";
import { GridLayout } from "..";

const ProjectTitle = ({ title }) => {
  return (
    <>
      <Title>{title}</Title>
    </>
  );
};

export default ProjectTitle;
